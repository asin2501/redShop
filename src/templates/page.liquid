<div class="main-container">
    <div class="container">
        <h1 class="page-title page-title--mb-65">{{ page.title }}</h1>

        <div class="text-page">
            {{ page.content }}
        </div>
    </div>
</div>